- name: create Jenkins jobs
  hosts: localhost
  connection: local
  vars:
    jobs:
      - roboshop-terraform
      - cart
      - catalogue
      - user
      - payment
      - shipping
  roles:
    - jenkins-jobs